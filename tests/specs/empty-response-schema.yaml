openapi: 3.1.0
info:
  title: Empty response schema
  version: 1.0.0
paths:
  /any:
    get:
      operationId: GetAny
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema: {}
